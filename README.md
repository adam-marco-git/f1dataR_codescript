## Installation

Install the package in RStudio using:

```r
install.packages("f1dataR")
```

Then load it:

```r
library(f1dataR)
```

## Python Dependency

`f1dataR` relies on the Python library **FastF1**, so you may also need to run:

```r
reticulate::py_install("fastf1")
```
